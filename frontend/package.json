{
  "name": "test-nixpacks-monorepo-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "npx serve -s dist",
    "build": "parcel build src/index.html --dist-dir dist"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "parcel": "^2.13.3",
    "@parcel/transformer-react-refresh": "^2.13.3",
    "process": "^0.11.10"
  }
}
